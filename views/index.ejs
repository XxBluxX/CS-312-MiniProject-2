<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- provides tab of webpage with a title -->
    <title>Cocktail Generator</title>
<!-- links the styles.css so the site will look pretty -->
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
<!-- div to style the cocktail information seperately -->
    <div class="container">
        <h1>Random Cocktail Generator: </h1>

<!-- using ejs to display the information the api sends back -->
        <h2><%= drink.strDrink %></h2>
        <img src="<%= drink.strDrinkThumb %>">

        <h3>Instructions: </h3>
        <p><%= drink.strInstructions %></p>

        <h3>Ingredients: </h3>
        <ul>
<!-- ejs to loop through the information and display it -->
            <% for (let i=1; i<=15; i++) { %>
                <% const ingredient = drink['strIngredient' + i]; %>
                <% const measure = drink['strMeasure' + i]; %>
                <% if (ingredient) { %>
                    <li><%= measure || '' %> <%= ingredient %></li>
                <% } %>
            <% } %>
        </ul>
<!-- form to generate a new random cocktail -->
        <form action="/" method="get">
            <button ype="submit">Get new cocktail</button>
        </form>
    </div>

</body>
</html>
